server:
  port: 8080

spring:
  datasource:
    username: root
    password: WinNut90!90A
    url: jdbc:mysql://localhost:3306/oilfield_lib?useSSL=false&requireSSL=false&allowPublicKeyRetrieval=true
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
